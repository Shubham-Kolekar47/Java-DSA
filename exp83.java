import java.util.*;//whenever we define a variable in the function then we can use that variable
//  in any part of the code aftre its defined(only in that function not after completeing of that 
// function or we canot use that variable even in other function aftr taht) we cannot use it brfore
//  it is defined(method scope)
public class exp83{
    public static int sum(int a,int b) {
        System.out.println(s);//it gives an error because here s is define after the execution  
        int s = a+b;
        return s;
        
    }
    public static void main(String[] args) {
        
    }
}

